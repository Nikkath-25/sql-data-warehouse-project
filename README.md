# Bronze_Silver_Gold_ETL_Project
Bronze → Silver → Gold ETL project simulating a data warehouse workflow with SQL. Demonstrates data cleaning, transformation, and aggregation from raw CSVs to business-ready tables.
